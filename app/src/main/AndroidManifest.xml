<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lursun.www.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".leftActivity"
            android:label="@string/title_activity_left"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".accumulation"
            android:label="@string/title_activity_accumulation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".progressupload"
            android:label="@string/title_activity_progressupload"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".update"
            android:label="@string/title_activity_update"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>